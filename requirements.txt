# Обязательное
python-docx>=1.1        # чтение и запись .docx

# Необязательное
qrcode>=7.4             # настоящие QR-коды вместо рамки-заглушки
pywin32>=306            # быстрый путь к MS Word для PDF (Windows)

# Ничего больше не нужно: PDF, штрихкоды, шрифты и разбор .frx —
# в самом проекте.
